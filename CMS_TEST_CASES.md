# سناریوهای تست CMS زیتو

این سناریوها برای اجرای محلی CMS هستند. قبل از شروع، سرور برنامه و worker
ایندکس RAG باید در حال اجرا باشند.

## پیش‌نیاز

1. Migrationها را اجرا کن: `alembic upgrade head`
2. برنامه را با `python -m uvicorn src.main:app --host 127.0.0.1 --port 8000` اجرا کن.
3. در یک ترمینال دیگر worker را اجرا کن:
   `python -m src.cli.rag_indexer --watch --limit 20`
4. با حساب مدیر محلی وارد `/admin` شو. اعتبار ورود از متغیرهای محیطی محلی خوانده می‌شود و نباید در این سند ثبت شود.

## TC-CMS-01: ساخت و تولید پیش‌نویس

1. در CMS «دوره جدید» را انتخاب کن.
2. نام، موضوع، هدف، مدت، مخاطب، گروه هدف، سطح، تعداد سرفصل، ساعت یادگیری و تعداد ایستگاه هر سرفصل را وارد کن.
3. برای تست کامل، `3` سرفصل و `9` ایستگاه انتخاب کن.
4. «تولید با AI» را بزن.

نتیجه مورد انتظار: یک پیش‌نویس نسخه ۱ با سه سرفصل و ۲۷ ایستگاه ساخته می‌شود. هر سرفصل باید مسیر یادگیری، خلاصه، فلش‌کارت، نکات، اشتباهات، مثال شخصی‌سازی‌شده، پرسش‌وپاسخ، ارزیابی و پایان سرفصل داشته باشد.

## TC-CMS-02: بازبینی و کنترل تغییرات

1. عنوان و توضیح یک سرفصل را ویرایش کن.
2. عنوان یا محتوای یک ایستگاه را تغییر بده.
3. یکی از فیلدهای مؤثر بر تولید، مانند هدف، نام دوره، مدت، تعداد سرفصل یا تعداد ایستگاه را تغییر بده.
4. تلاش کن پیش‌نویس را منتشر کنی.

نتیجه مورد انتظار: ویرایش محتوایی ذخیره می‌شود. پس از تغییر چارچوب تولید، وضعیت «نیاز به تولید مجدد» دیده می‌شود و انتشار تا اجرای دوباره تولید AI مسدود است.

## TC-CMS-03: انتشار، لندینگ و فهرست دوره‌ها

1. پیش‌نویس را پس از تولید نهایی منتشر کن.
2. صفحه `/app/` را باز کن و فهرست دوره‌ها را تازه‌سازی کن.
3. روی دوره جدید بزن و آدرس `/app/courses/<slug>` را بررسی کن.

نتیجه مورد انتظار: دوره بدون تغییر دستی در فهرست ظاهر می‌شود؛ لندینگ از همان اطلاعات نسخه منتشرشده، سرفصل‌ها و تعداد ایستگاه‌ها ساخته می‌شود. در مسیر باریک و موبایل، نقشه و تعداد ایستگاه‌ها حفظ می‌شود.

## TC-CMS-04: آماده شدن دانش کوچ

1. پس از انتشار، در CMS وضعیت «دانش کوچ» را بررسی کن.
2. صبر کن تا وضعیت از «در صف/در حال ایندکس» به «آماده پاسخ‌گویی» برسد.
3. با یک کاربر یادگیرنده وارد شو، دوره را انتخاب کن و یک پرسش دقیق درباره نخستین سرفصل بپرس.

نتیجه مورد انتظار: برای هر سرفصل یک منبع KB و یک job ایندکس ایجاد می‌شود. پاسخ کوچ `grounded` است و citation متعلق به همان دوره و سرفصل دارد. تا آماده شدن ایندکس، انتشار برقرار است اما پاسخ کوچ نباید بدون منبع ادعای قطعی بسازد.

## TC-CMS-05: نسخه جدید بدون آسیب به یادگیرنده فعال

1. برای دوره منتشرشده «نسخه جدید» بساز.
2. عنوان یا یک ایستگاه را در نسخه جدید تغییر بده، سپس تولید/بازبینی و انتشار را کامل کن.
3. دوره را هم با کاربر جدید و هم با کاربری که پیش‌تر ثبت‌نام کرده باز کن.

نتیجه مورد انتظار: کاربر جدید نسخه تازه را می‌بیند. کاربر فعال، نسخه‌ای را که با آن ثبت‌نام کرده ادامه می‌دهد؛ عنوان، مسیر، محتوا و منبع‌های RAG او تغییر نمی‌کنند.

## چک‌های خطا

- تولید AI ناموفق: خطا باید در خود پیش‌نویس ثبت شود و انتشار مسدود بماند.
- worker خاموش: وضعیت RAG در CMS نباید «آماده» نمایش داده شود.
- ویرایش نسخه منتشرشده: API و UI باید آن را رد کنند و تنها ساخت نسخه جدید مجاز باشد.
- تعداد ایستگاه خارج از ۷، ۸ یا ۹: API باید خطای اعتبارسنجی بدهد.
