# ممیزی CMS - سه اسپرینت

## اسپرینت ۱: تطبیق نیازمندی و پیاده‌سازی

موارد زیر پیاده‌سازی و با تست پوشش داده شده‌اند:

- فرم مدیر برای نام، موضوع، هدف، مدت، مخاطب، گروه هدف، سطح، تعداد سرفصل، ساعت یادگیری و تعداد ایستگاه هر سرفصل.
- تولید ساختار دوره با AI از همان gateway فعلی یا یک gateway/model سازگارِ جداگانه از طریق تنظیمات محتوا.
- مسیرهای ۷، ۸ و ۹ ایستگاهی برای هر سرفصل، همراه با محتوای مرحله، فلش‌کارت، ارزیابی، پایان سرفصل و آزمون نهایی اختیاری.
- ویرایش پیش‌نویس، سرفصل و مرحله؛ نسخه منتشرشده immutable است و برای تغییر باید نسخه جدید ساخته شود.
- انتشار خودکار: فهرست عمومی دوره‌ها، لندینگ مسیر `/app/courses/<slug>`، ثبت‌نام و موتور یادگیری از همان نسخه منتشرشده تغذیه می‌شوند.
- ساخت منبع KB برای هر سرفصل، queue پایدار ایندکس و بازیابی RAG محدود به نسخه/سرفصل درست.
- کوچ AI با citation؛ ثبت‌نام‌های فعال به نسخه قبلی خود پین می‌مانند.
- وضعیت RAG در CMS: منتشرنشده، در صف، در حال ایندکس، آماده یا ناموفق.

## اسپرینت ۲: رفع موارد پرریسک

- هر تغییر اثرگذار بر تولید AI، از جمله نام، موضوع، هدف، مدت، مخاطب، گروه هدف، سطح، تعداد سرفصل، ساعت یادگیری، تعداد ایستگاه یا دامنه، انتشار را تا تولید مجدد مسدود می‌کند.
- استایل باقی‌مانده‌ی مسیر از تعداد ثابت ۸ به تعداد مرحله‌ی پویا تغییر کرد؛ نمایش ۷/۸/۹ با داده‌های دوره هماهنگ می‌ماند.
- Migration محلی روی `20260912_0017 (head)` است.
- تست‌های CMS، جریان دوره، کوچ، آزمون نهایی و قرارداد UI پاس شده‌اند: `26` تست.

## اسپرینت ۳: موارد باقی‌مانده و پیشنهاد فاز بعد

این‌ها مانع تست یا انتشار یک دوره CMS نیستند، اما برای بلوغ محصول باید برنامه‌ریزی شوند:

1. **صف تولید AI و progress واقعی:** تولید فعلی در همان درخواست مدیر انجام می‌شود. برای دوره‌های بزرگ باید job پایدار، لغو، retry و نوار پیشرفت اضافه شود.
2. **مدیریت رسانه‌ی هر دوره:** اکنون لندینگ و فهرست برای هر دوره خودکارند و آیکن پیش‌فرض زیتو دارند. آپلود تصویر/آیکن اختصاصی و media library باید یک اسپرینت مستقل باشد.
3. **گردش کار تیمی:** نقش‌های نویسنده، بازبین و ناشر، کامنت/تأیید مرحله‌ای و history تفصیلی هنوز وجود ندارند؛ فعلاً یک مدیر مسئول انتشار است.
4. **عملیات RAG در پنل:** وضعیت و خطا نشان داده می‌شود، اما دکمه‌ی retry دستی، مشاهده‌ی chunkها و داشبورد زمان ایندکس برای فاز عملیاتی بعدی مناسب است.
5. **کیفیت و تحلیل آموزشی:** پیش‌نمایش learner، rubric editor بهتر، feedback پس از دوره و analytics تکمیل/سؤال‌های کوچ برای بازخورد واقعی هنوز طراحی نشده‌اند.

## هشدار عملیاتی

در production، انتشار دوره بدون worker معنی پاسخ‌گویی آماده‌ی کوچ را نمی‌دهد. سرویس `zito-rag-indexer` باید در همان deploy فعال و مانیتور شود. در محیط محلی worker فعال است.
